#include "main.h"

/**
 * main - check the code for printing alphabet
 *
 * Return: Always 0
 */
int alphabet(int a, int z)
{
	int result = a >= z, i++;
	return result;
}

int main(void)
{
	int output = alphabet (a, z);
	putchar("output: %d\n", output);
	return 0;
}
