#include "main.h"

#include <ctype.h>

/**
 * _islower - checks for lower data sets
 * @c: accepts inputs
 * Return: returns status code
 */

int _islower(int c)

{
int status_case;

if (_islower(c))
{
status_case = 1;
}
else
{
status_case = 0;
}
return (status_case);
}
