#include "main.h"

#include <ctype.h>

/**
 * _islower - checks for lower data sets
 * @c: accepts inputs
 * Return: returns status code
 */

int _islower(int c)

{
int value;

if (_islower(c))
{
value = 1;
}
else
{
value = 0;
}
return (value);
}
