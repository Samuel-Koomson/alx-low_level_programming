#include "main.h"

/**
* mul - multiplies two integers and returns results
* @a: first integer
* @b: second integer
* Return: Returns the results of a * b
*/

int mul(int a, int b)
{
return (a * b);
}
